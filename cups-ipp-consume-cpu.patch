diff -up cups-1.4.2/cups/request.c.ipp-consume-cpu cups-1.4.2/cups/request.c
--- cups-1.4.2/cups/request.c.ipp-consume-cpu	2017-04-27 11:26:53.276820631 +0200
+++ cups-1.4.2/cups/request.c	2017-04-27 11:29:19.677661720 +0200
@@ -263,14 +263,16 @@ cupsDoIORequest(http_t     *http,	/* I -
       response = cupsGetResponse(http, resource);
       status   = http->status;
     }
+    else
+      httpFlush(http);
 
     DEBUG_printf(("2cupsDoIORequest: status=%d", status));
 
-    if (status >= HTTP_BAD_REQUEST &&
-	status != HTTP_UNAUTHORIZED &&
-	status != HTTP_UPGRADE_REQUIRED)
+    if (status == HTTP_ERROR ||
+        (status >= HTTP_BAD_REQUEST &&
+         status != HTTP_UNAUTHORIZED &&
+         status != HTTP_UPGRADE_REQUIRED))
     {
-      httpFlush(http);
       _cupsSetHTTPError(status);
       break;
     }
